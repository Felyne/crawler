syntax = "proto3";

package pb;

enum ServiceName {
    ITEMSAVER_SERVICE = 0;
}

//存储的一条记录
message Item {
    string Url = 1;
    string Type = 2;
    string Id = 3;
    bytes Payload = 4;
}

message Resp {
    string result = 1;
}

service ItemSaver {
    rpc Save(Item) returns (Resp) {}
}